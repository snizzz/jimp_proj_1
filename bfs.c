#include <stdio.h>
#include <stdbool.h>
#include "bfs.h" 

bool if_connected(struct Graph* graph)
{
	bool *odwiedzone = malloc(graph->k*graph->w*sizeof(bool));	
}

void path_bfs(struct Graph *graph, int start, int dest)
{


}

